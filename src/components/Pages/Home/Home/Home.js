import { Box } from '@mui/system';
import React from 'react';
import Header from '../Header/Header';

const Home = () => {
    return (
        <Box>
            <Header/>
        </Box>
    );
};

export default Home;